<Window x:Class="DWS.Console.Forms.TaskOverview.TaskOverviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DWS.Console.Forms.TaskOverview.NewTask"
        mc:Ignorable="d"
        Title="Overview tasks" Height="450" Width="800">
    <Window.Resources>
        <CollectionViewSource x:Key="Tasks" Source="{Binding Tasks}" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid 
            Grid.Row="0"
            Margin="10"            
            ItemsSource="{Binding Tasks}"
            SelectedItem="{Binding SelectedTask}"
            AutoGenerateColumns="False"
            CanUserAddRows="False" >

            <DataGrid.Columns>

                <DataGridTemplateColumn  Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Margin="10" Padding="5" Content="Delete" Command="{Binding DeleteTaskCommand}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Clientname" Width="400">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding ClientName}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTemplateColumn Header="yardname" Width="400">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding YardName}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>

        </DataGrid>

        <Button 
            Grid.Row="1" 
            Margin="10" Padding="5,0" Content="Add task" Click="AddTask_Click"   />
      

    </Grid>
</Window>
